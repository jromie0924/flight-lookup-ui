# Copy this file to `.env` and fill in the values. `.env` is gitignored.

# Base URL of the flight-query API (API Gateway stage URL or custom domain).
# No trailing slash. The app requests `${VITE_API_BASE_URL}/flights`.
VITE_API_BASE_URL=https://api.example.com

# API key appended as the `key` query parameter. In production, `infra/deploy.sh`
# pulls this from SSM (/plane-tracker/api/key in us-east-2), so you only need to
# set it locally for `npm run dev`.
VITE_API_KEY=
